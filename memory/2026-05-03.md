# 2026-05-03 — Nhật ký Sóc 🐿️

## 🎯 Mục tiêu hôm nay
- [x] Tích hợp đa giao thức (Protocol: Local/FTP) vào Zustand Store.
- [x] Kết nối UI FtpDialog với logic kết nối thực tế.
- [x] Cập nhật FilePane để hiển thị đúng dữ liệu từ FTP Server.
- [x] Fix lỗi Panel thông tin file không reactive khi chuyển đổi Pane.
- [x] **Phase 7**: Thao tác file chéo giao thức (Copy/Move Local <-> FTP).

## 🛠 Những việc đã làm
1. **Backend (Rust)**:
   - Thêm lệnh `ftp_mkdir`, `ftp_delete`, `ftp_rename` để thao tác trực tiếp trên server.
2. **App.tsx**:
   - Nâng cấp logic phím tắt F5 (Copy), F6 (Move), F7 (New Folder), F8 (Delete) để tự động nhận diện Protocol.
   - Hỗ trợ Upload (Local->FTP) và Download (FTP->Local) tự động khi nhấn F5/F6 giữa 2 vùng khác nhau.
3. **FilePane.tsx & ContextMenu.tsx**:
   - Đồng bộ logic Context Menu (chuột phải) với hệ thống chéo giao thức.
   - Vô hiệu hóa các tính năng chỉ dành cho Local (như Mở trong Terminal) khi đang ở chế độ FTP.

## ⚠️ Lưu ý kỹ thuật
- Di chuyển file (F6) chéo giao thức được thực hiện bằng cách Copy + Delete.
- Copy FTP -> FTP hiện chưa hỗ trợ (do hầu hết FTP server không cho phép Copy trực tiếp, cần cơ chế Download -> Upload trung gian).

## ⏩ Bước tiếp theo
- [ ] Thử nghiệm thực tế với server FTP thật (ProFTPD/vsftpd).
- [ ] Cải thiện UI hiển thị tiến trình (Progress Bar) khi Upload/Download file lớn.
- [ ] Thêm tính năng "Quick Edit" (Download -> Edit -> Auto Upload).

---
*Ký tên: Sóc 🐿️*
