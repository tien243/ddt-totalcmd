# 2026-05-01 — DDT Commander Session Log

## Phiên làm việc: Phase 3-5 (Build → Test → Debug)

### ✅ Công việc hoàn thành

1. **Cập nhật Rust 1.86 → 1.95.0** (via `brew install rust`)
   - Fix lỗi dependency conflict: `darling`, `serde_with`, `time`, `plist` yêu cầu Rust ≥1.88

2. **Backend Rust — Hoàn thiện**
   - `list_directory`: Quét thư mục, sắp xếp DIR trước, alpha
   - `create_directory`, `delete_item`, `move_item`, `copy_item` (recursive cho thư mục)
   - `get_home_dir`: Lấy $HOME
   - Đăng ký `tauri_plugin_dialog::init()` trong Builder

3. **Frontend — Hoàn thiện**
   - `FilePane.tsx`: Viết lại KHÔNG dùng `react-window` (xung đột React 19). Dùng native `overflow-y-auto` + `scrollIntoView()`
   - `ActionBar.tsx`: F3-F8 buttons
   - `App.tsx`: Global keyboard handler (Tab, F5-F8, Delete) với `window.confirm/prompt`
   - `useFileStore.ts`: Zustand store quản lý dual-pane state

4. **Fix lỗi Tailwind CSS v4 không hoạt động** ⚠️ CRITICAL
   - **Root cause**: Thiếu `@tailwindcss/vite` plugin trong `vite.config.ts`
   - **Fix**: `npm install @tailwindcss/vite` + thêm `tailwindcss()` vào plugins array
   - **Bài học**: Tailwind v4 BẮT BUỘC phải có `@tailwindcss/vite` (hoặc `@tailwindcss/postcss`). Chỉ `@import "tailwindcss"` trong CSS là KHÔNG ĐỦ

5. **Fix lỗi trắng trang**
   - Loại bỏ `react-window` v1.8 + `react-virtualized-auto-sizer` (peer dep chỉ React ≤18)
   - Xóa CSS mặc định Vite template (`body { display: flex; place-items: center }`)
   - Thêm `dialog:default` permission vào `src-tauri/capabilities/default.json`

### 🔑 Constraints & Gotchas đã phát hiện
- **React 19 + react-window v1.8** = CRASH. Peer dep conflict. Phải dùng native scroll hoặc chờ react-window v2
- **Tailwind CSS v4** = PHẢI có vite/postcss plugin. `@import "tailwindcss"` chỉ là CSS syntax, plugin mới xử lý được
- **Tauri v2 plugins** = Phải đăng ký cả Rust side (`.plugin()`) VÀ capabilities (`default.json`)
- **npm** với React 19 = Luôn cần `--legacy-peer-deps` cho nhiều packages

### 📁 Key Files
| File | Vai trò |
|------|---------|
| `src-tauri/src/lib.rs` | Rust commands + plugin registration |
| `src/App.tsx` | Layout chính + global keyboard handler |
| `src/components/FilePane.tsx` | Component hiển thị file list (native scroll) |
| `src/components/ActionBar.tsx` | F-key action buttons |
| `src/store/useFileStore.ts` | Zustand state management |
| `src/index.css` | Tailwind v4 import + base styles |
| `vite.config.ts` | Vite + React + Tailwind plugins |
| `src-tauri/capabilities/default.json` | Tauri permissions |

### 📊 Trạng thái hiện tại
- **Phase**: 5 (Testing) — App chạy được, layout hiển thị đúng
- **Pending**: User verification các tính năng F-key, keyboard navigation
- **Next**: Phase 6 (Iterative Refinement) dựa trên feedback
